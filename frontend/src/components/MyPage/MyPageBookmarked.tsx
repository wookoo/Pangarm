import { PrecedentItemType } from "@/types";
import { useEffect } from "react";
import PrecedentItem from "../PrecedentItem";

export default function MyPageBookmarked() {
  useEffect(() => {}, []);
  return <div></div>;
}
