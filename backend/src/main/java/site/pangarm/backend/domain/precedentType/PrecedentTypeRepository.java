package site.pangarm.backend.domain.precedentType;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PrecedentTypeRepository extends JpaRepository<PrecedentType,Integer> {
}
